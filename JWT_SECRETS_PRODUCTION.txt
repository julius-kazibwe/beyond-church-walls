# Production JWT Secrets

⚠️  KEEP THESE SECRET! Do not commit to Git or share publicly.

## Generated Secrets

### JWT_SECRET (for user authentication)
NTovEYS9PZaWOCAIwhsc3Xv3Z8qCkFfpMC3wmjRYe80=

### ADMIN_JWT_SECRET (for admin authentication)
IJ//QexOkBU9iUPMrBiXsMOx2xbIIAg9M6VCYUMDg3A=

## How to Use in Render

1. Go to your Render dashboard
2. Navigate to your service: `beyond-church-walls-api`
3. Go to **Environment** tab
4. Add these environment variables:

   **JWT_SECRET**
   - Key: `JWT_SECRET`
   - Value: `NTovEYS9PZaWOCAIwhsc3Xv3Z8qCkFfpMC3wmjRYe80=`
   - Sync: false (do not sync)

   **ADMIN_JWT_SECRET**
   - Key: `ADMIN_JWT_SECRET`
   - Value: `IJ//QexOkBU9iUPMrBiXsMOx2xbIIAg9M6VCYUMDg3A=`
   - Sync: false (do not sync)

5. Save and redeploy your service

## Security Notes

- ✅ These are cryptographically secure random secrets
- ✅ Each secret is 32 bytes (256 bits) encoded in base64
- ✅ Never share these secrets publicly
- ✅ Never commit them to version control
- ✅ Use different secrets for different environments (dev/staging/prod)
- ✅ If compromised, generate new secrets and invalidate all existing tokens

## Verification

After setting these in Render, check the logs to ensure:
- No warning about default JWT_SECRET
- Server starts successfully
- Authentication works correctly

## If You Need to Regenerate

Run these commands:
```bash
openssl rand -base64 32  # For JWT_SECRET
openssl rand -base64 32  # For ADMIN_JWT_SECRET
```

